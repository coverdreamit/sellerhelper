# 백엔드 API 서버 URL (next.config.mjs rewrites에서 사용)
# 변경 후 개발 서버 재시작 필요: yarn dev / npm run dev
NEXT_PUBLIC_API_URL=http://localhost:5080

# 포털(인증) 서버 URL. 지정하지 않으면 NEXT_PUBLIC_API_URL 사용
# NEXT_PUBLIC_PORTAL_URL=http://localhost:5080
