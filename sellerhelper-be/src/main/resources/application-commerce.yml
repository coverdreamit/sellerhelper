# 커머스 서비스 - 스토어/몰/상품/주문 API (JWT 검증)
# 사용: SPRING_PROFILES_ACTIVE=commerce
# 로컬: 기본 5001 (FE 호출과 동일). 스케일 배포 시 SERVER_PORT=5080 등 지정

spring:
  application:
    name: sellerhelper-commerce

server:
  port: ${SERVER_PORT:5001}
